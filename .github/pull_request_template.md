## Summary
- Describe the change in a sentence or two
- Explain how this keeps the OpenClaw/Codex integration aligned if relevant

## Testing
- List the commands you ran (
  e.g., `python -m compileall src`, `exom index --notes-root ./ --out-root .neural`)

## Checklist
- [ ] `CHANGELOG.md` updated with this change under `Unreleased`
- [ ] CI passes (see `.github/workflows/ci.yml`)
